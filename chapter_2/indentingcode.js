/** @format */

// Indentation inside blocks is to make the structure of the code stand out
//makes easy to read and understand

if (false != true) {
	console.log('That makes sense');

	if (1 < 2) {
		console.log('There is no surprise');
	}
}
